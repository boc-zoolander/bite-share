
// Only the host would see

// After host picks a restaurant

//    (DB/server GENERATES UNIQUE SESSION ID and RETURNS)

// Host enters a phoen number and hits "send"

// Allow host to enter phone numbers to text? links to